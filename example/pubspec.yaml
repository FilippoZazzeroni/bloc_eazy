name: example
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  bloc_eazy:
    path: "../../bloc"
  flutter_bloc: ^8.0.1
  ui_mixins:
    git: "https://github.com/FilippoZazzeroni/ui_mixins.git"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true
